[project]
name = "ppi-redactor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.13.0",
    "torch>=2.11.0",
    "transformers>=5.7.0",
]
